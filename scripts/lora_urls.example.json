{
 "_note": "LoRA name -> fal storage URL. Copy this file to lora_urls.json and fill in your own. Running `lookshot.py frames --lora <path>.safetensors` once uploads your weights to fal storage and registers the key here automatically. Trained weights are never distributed with this repo.",
 "v2_3000": "<fal storage URL>"
}
